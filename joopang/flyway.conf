# Default Flyway configuration used by the CLI.
# Override these values with --url= / --user= / --password= options or by editing this file per environment.

flyway.url=jdbc:mysql://localhost:3311/joopang?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
flyway.user=joopang
flyway.password=joopang
flyway.schemas=joopang
flyway.locations=filesystem:src/main/resources/db/migration