apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: joopang
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
        version: v1
        istio: enabled
    spec:
      initContainers:
        - name: wait-for-order-mysql
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z order-mysql 3306; do echo "waiting for order-mysql"; sleep 3; done']
        - name: wait-for-redis
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z redis 6379; do echo "waiting for redis"; sleep 3; done']
      containers:
        - name: order-service
          image: ghcr.io/example/joopang-order-service:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: joopang-shared-config
          env:
            - name: SERVER_PORT
              value: "8083"
            - name: DB_URL
              value: jdbc:mysql://order-mysql:3306/order_service?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: order-mysql-secret
                  key: mysql-username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: order-mysql-secret
                  key: mysql-password
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8083
            initialDelaySeconds: 60
            periodSeconds: 15
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
